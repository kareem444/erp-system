import { IInputsConstants } from "src/common/interfaces/InputsInterface";
import { IAdminStoreInputs } from "../interfaces/AdminStoresInterface";

export const AdminStoreInputsConstants: IInputsConstants<IAdminStoreInputs> = {
    stockname: 'stockname',
    accountno: 'accountno'
}